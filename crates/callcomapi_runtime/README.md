# callcomapi_runtime

> **注意**：这是 `callcomapi` 的内部运行时支持 crate。大多数用户应该使用更高层级的 [callcomapi](https://crates.io/crates/callcomapi) crate。

`callcomapi_runtime` 实现了 `callcomapi_macros` 所需的后台线程管理、消息传递和 COM 初始化逻辑。
