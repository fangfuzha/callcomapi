# callcomapi_runtime

> **Note**: This is the internal runtime support crate for `callcomapi`. Most users should use the higher-level [callcomapi](https://crates.io/crates/callcomapi) crate instead.

`callcomapi_runtime` implements the background thread management, messaging, and COM initialization
logic required by `callcomapi_macros`.
